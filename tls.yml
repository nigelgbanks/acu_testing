tls:
  stores:
    default:
      defaultCertificate:
        certFile: /etc/ssl/traefik/fullchain.pem
        keyFile: /etc/ssl/traefik/privkey.pem
  certificates:
    - certFile: /etc/ssl/traefik/fullchain.pem
      keyFile: /etc/ssl/traefik/privkey.pem
